# Related repositories

Sibling repositories referenced by, but not part of, this system. Consult these instead of duplicating their content into `knowledge/`. When a topic already lives in a sibling, link to it rather than re-capturing.

| Repo | Path | Purpose | Format |
| ---- | ---- | ------- | ------ |
| | | | |

## Scope rule

- Technical snippet, command-line fix, or curated link list → check the owning sibling repo first; link to the page rather than re-file in `knowledge/raw/`.
- Personal preference, working context, process documents, active project work, customer history → this repo.

## Adding a sibling

Append a row to the table above. Keep it to path + purpose + format — no content mirroring.
